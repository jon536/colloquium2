<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.eugene.colloquium2" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MyActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChangeItem"
            android:label="@string/title_activity_change_item" >
        </activity>
        <activity
            android:name=".Vote"
            android:label="@string/title_activity_vote" >
        </activity>
        <activity
            android:name=".ResultOfVote"
            android:label="@string/title_activity_result_of_vote" >
        </activity>
    </application>

</manifest>
